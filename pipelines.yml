pipelines:
  - name: pipeline_hello_world
    steps:
      - name: step_1
        type: Bash
        execution:
          onStart:
            - echo "this is onStart"
          onExecute:
            - asd
            - printenv
          onSuccess:
            - echo "this is onSuccess"
          onComplete:
            - asd
          onFailure:
            - echo "this is onFailure"
